Meteor.Router.add({
    /****************************************************************
      When external sources post to the url 
      we render the corresponding template
    ****************************************************************/    
    '/': 'carousel'
});